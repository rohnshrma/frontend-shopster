import "../App.css"

const Header = ()=>{
    return(
        <header>
            <button><i class="fa-solid fa-right-from-bracket"></i> Logout</button>
        </header>
    )
}

export default Header